<%= render 'navigation' %>

<div class="container">
	<div class="row">
		<p></p>
		<h1>Cat Table</h1>
		<table class="table table-bordered table-responsive table-striped text-center">
			<thead>
				<tr>
					<th>ID</th>
					<th>Name</th>
					<th>Breed</th>
					<th>Age</th>
					<th>Opsi</th>
				</tr>
			</thead>
			<tbody>
				<% @cats.each do |cat| %>
				<tr>
					<td><%= cat.id %></td>
					<td><%= cat.name %></td>
					<td><%= cat.breed %></td>
					<td><%= cat.age %></td>
					<td>
						<%= link_to 'Detail', cat_path(cat), class:'btn btn-info' %>
						<%= link_to 'Edit', cat_path(cat), class:'btn btn-primary' %>
						<%= link_to 'Delete', cat_path(cat), class:'btn btn-danger' %>
					</td>
				</tr>
				<% end %>
			</tbody>
		</table>
	</div>
</div>
